export function sayWoy(name) {
    console.log(`Woii!! ${name}`);
}

export function sayDadah(name) {
    console.log(`Dadah ${name}`);
}
const name = "Adek"

/* 
    --- EXPORT
    Kata kunci export merupakan kata kunci yang digunakan untuk mengekspose kode dari sebuah module
    export bisa ditambahkan di awal sebuah variabel, function atau class
*/