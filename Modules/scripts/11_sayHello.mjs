export default function (name) {
    console.log(`hello ${name}, from default function`);
}

/* export default company = "PT. CINTA SEJATI";

export default class Company {
    constructor(name){
        this.name = name
    }
    sayHii(){
        console.log(`Hii ${name} , ini gue ${this.name}`)
    }
} */

